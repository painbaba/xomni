name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-matrix:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11']
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Run full test matrix (1043 tests / 29 plugins)
        run: bash .bench/run_all_tests.sh

  test-windows:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11']
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      # run_all_tests.sh is bash-only -> drive it through git-bash (preinstalled
      # on windows-latest runners). Windows-specific env checks run first so a
      # broken npx shim / read-only config / path bug / spaces-path launch /
      # host-config writer regression fails fast with a clear name.
      - name: Windows environment checks
        shell: bash
        run: python .bench/windows_checks.py
      - name: Run full test matrix (1043 tests / 29 plugins, git-bash)
        shell: bash
        run: bash .bench/run_all_tests.sh
