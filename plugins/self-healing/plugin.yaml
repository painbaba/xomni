name: self-healing
version: "0.2.0"
description: "Self-healing agent (U9/U-ASSURE-2): watchdog kills silent hangs, postcondition checks catch exit-0-nothing-happened, multi-profile config-drift scan + auto-fix (base + all profiles/*), per-profile audit trail heal.jsonl, /heal profiles|scan|fix commands — zero hooks"
