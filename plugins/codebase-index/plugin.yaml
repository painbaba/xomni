name: codebase-index
version: "0.2.0"
description: "Hybrid incremental codebase index (repomap v2): SQLite FTS5 trigram, stat-diff + content-hash incremental updates, BM25 + symbol-boost ranking. /cindex + codebase_query tool. Zero hooks."
