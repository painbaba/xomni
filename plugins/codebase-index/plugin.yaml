name: codebase-index
version: "0.3.0"
description: "Hybrid incremental codebase index (repomap v2): SQLite FTS5 trigram, stat-diff + content-hash incremental updates, BM25 + symbol-boost ranking, opt-in Ollama embeddings with RRF hybrid query (/cindex query --hybrid). /cindex + codebase_query tool. Zero hooks."
