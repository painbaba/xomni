name: omni-skills
version: "1.0.0"
description: "XOMNI SKILL.md interop — scan, security-validate, and install Hermes-format + foreign SKILL.md skills from any marketplace (/skills-scan, /skills-install). Zero hooks, zero per-turn cost."
