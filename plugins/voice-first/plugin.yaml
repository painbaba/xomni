name: voice-first
version: "0.2.0"
description: "Optional voice-first mode: hands-free CLI via mic capture (ffmpeg/arecord), pluggable STT (whisper-local|gemini|sarvam) + TTS (edge|sarvam|bhashini) backend registry with /voice backends + /voice set; /voice test|ask|on — zero hooks"
