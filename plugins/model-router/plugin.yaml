name: model-router
version: "0.1.0"
description: "Automatic per-task model routing over omni-registry capabilities (quick/reasoning/vision/heavy) + cost-tracker-ledger telemetry; /route commands — zero hooks"
