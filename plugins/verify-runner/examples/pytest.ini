[pytest]
# pytest 9 defaults to importlib import mode, which breaks on this project's
# parent package chain (xomni/__init__.py + the hyphenated verify-runner dir).
# prepend keeps test modules top-level so `from example_math import ...` works.
import-mode = prepend
